# AWS BloodBridge Environment Configuration
# Account ID: 904233124678
# Region: us-east-1

# ==================== AWS CONFIGURATION ====================
AWS_REGION=us-east-1
BLOOD_REQUEST_SNS_TOPIC_ARN=arn:aws:sns:us-east-1:904233124678:sns_topic:d0354386-8de3-40fe-a949-a28c74e35a29

# ==================== DYNAMODB TABLES ====================
# These are the default table names used by the application
# Make sure these tables exist in DynamoDB with correct partition keys:
# - BloodBridgeUsers: partition key = user_id (String)
# - BloodBridgeDonations: partition key = donation_id (String)
# - BloodBridgeRequests: partition key = request_id (String)
# - BloodBridgeMessages: partition key = message_id (String)

USERS_TABLE=BloodBridgeUsers
DONATIONS_TABLE=BloodBridgeDonations
REQUESTS_TABLE=BloodBridgeRequests
MESSAGES_TABLE=BloodBridgeMessages

# ==================== APPLICATION SETTINGS ====================
# IMPORTANT: Change this to a secure random string in production!
FLASK_SECRET_KEY=blood_bridge_secret_key_CHANGE_ME_IN_PRODUCTION

# Application port (default: 5000)
PORT=5000

# Debug mode (set to False in production)
FLASK_DEBUG=False

# ==================== DEPLOYMENT NOTES ====================
# 1. Create all 4 DynamoDB tables with correct partition keys
# 2. Create SNS topic and add email subscriptions
# 3. Create IAM role using IAM_POLICY_TEMPLATE.json
# 4. Attach IAM role to EC2 instance
# 5. Set these environment variables on EC2 instance
# 6. For production: use gunicorn + nginx instead of Flask dev server
